#include "main.h"
/**
 * _islower - programing checking lowercase character
 * @c: character to check
 * Return: 1 if ci lowercase, and 0 if not
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z')
}
